# Configuration for Data Warehouse Updates
# Copy this file to config.yml and fill in your actual values
# The config.yml file is gitignored and will not be committed to the repo

# Plant Species Metadata Update Configuration
# Used by: notebooks/update_plant_species_metadata.ipynb
plant_species:
  gcs:
    # URL to the plant species CSV file in Google Cloud Storage
    # Format: gs://bucket-name/path/to/file.csv
    csv_url: "gs://your-bucket/path/to/plant_species.csv"
    
    # Backup location for table backups (optional but recommended)
    backup_bucket: "your-bucket"
    backup_prefix: "backups/plant_species"
  
  bigquery:
    # Full table ID for the plant species metadata table
    # Format: project.dataset.table
    table_id: "your-project.your_dataset.plant_species_table"
    
    # Optional: Specify project explicitly if needed
    project: "your-project-id"

# GridVeg Survey Metadata Update Configuration
# Used by: notebooks/update_gridVeg_survey_metadata.ipynb
gridveg_survey_metadata:
  gcs:
    # URL to the gridVeg survey metadata CSV file in Google Cloud Storage
    csv_url: "gs://your-bucket/path/to/gridVeg_survey_metadata.csv"
    
    # Backup location for table backups (optional but recommended)
    backup_bucket: "your-bucket"
    backup_prefix: "gridVeg/bak"
  
  bigquery:
    # Full table ID for the gridVeg survey metadata table
    # Format: project.dataset.table
    table_id: "your-project.your_dataset.gridVeg_survey_metadata"
    
    # Optional: Specify project explicitly if needed
    project: "your-project-id"

# GridVeg Point Intercepts Update Configuration
# Used by: notebooks/update_gridVeg_point_intercepts.ipynb
gridveg_point_intercepts:
  gcs:
    # URL to the gridVeg point intercepts CSV file in Google Cloud Storage
    csv_url: "gs://your-bucket/path/to/gridVeg_point_intercepts.csv"
    
    # Backup location for table backups (optional but recommended)
    backup_bucket: "your-bucket"
    backup_prefix: "gridVeg/bak"
  
  bigquery:
    # Full table IDs for the gridVeg point intercept tables
    # This CSV feeds two different tables
    table_vegetation: "your-project.your_dataset.gridVeg_point_intercept_vegetation"
    table_ground: "your-project.your_dataset.gridVeg_point_intercept_ground"
    
    # Optional: Specify project explicitly if needed
    project: "your-project-id"

# GridVeg Additional Species Update Configuration
# Used by: notebooks/update_gridVeg_additional_species.ipynb
gridveg_additional_species:
  gcs:
    # URL to the gridVeg additional species CSV file in Google Cloud Storage
    csv_url: "gs://your-bucket/path/to/gridVeg_additional_species.csv"
    
    # Backup location for table backups (optional but recommended)
    backup_bucket: "your-bucket"
    backup_prefix: "backups/gridveg_additional_species"
  
  bigquery:
    # Full table ID for the gridVeg additional species table
    # Format: project.dataset.table
    table_id: "your-project.your_dataset.gridVeg_additional_species"
    
    # Optional: Specify project explicitly if needed
    project: "your-project-id"

# GridVeg Image Metadata Update Configuration
# Used by: notebooks/update_gridVeg_image_metadata.ipynb
gridveg_image_metadata:
  gcs:
    # URL to the gridVeg image metadata CSV file in Google Cloud Storage
    csv_url: "gs://your-bucket/path/to/gridVeg_ref_image_metadata.csv"
    
    # Backup location for table backups (optional but recommended)
    backup_bucket: "your-bucket"
    backup_prefix: "gridVeg/bak"
  
  bigquery:
    # Full table ID for the gridVeg image metadata table
    # Format: project.dataset.table
    table_id: "your-project.your_dataset.gridVeg_image_metadata"
    
    # Optional: Specify project explicitly if needed
    project: "your-project-id"

# CSV Append Configuration
# Used by: notebooks/append_csv_to_bq.ipynb
# Generic configuration for appending CSV data to BigQuery tables
csv_append:
  gcs:
    # URL to the CSV file in Google Cloud Storage
    csv_url: "gs://your-bucket/path/to/data.csv"
    
    # Backup location for table backups (optional but recommended)
    backup_bucket: "your-bucket"
    backup_prefix: "backups/csv_append"
  
  bigquery:
    # Full table ID for the target table
    # Format: project.dataset.table
    table_id: "your-project.your_dataset.your_table"
    
    # Optional: Specify project explicitly if needed
    project: "your-project-id"

# GridVeg Plant Functional Groups Append Configuration
# Used by: notebooks/append_csv_to_bq.ipynb
gridveg_plant_functional_groups_append:
  gcs:
    csv_url: "gs://mpg-data-warehouse/gridVeg/src/2025/gridVeg_plant_functional_groups-WRANGLE-251104.csv"
    backup_bucket: "mpg-data-warehouse-backups"
    backup_prefix: "backups/vegetation_gridVeg_summaries/gridVeg_plant_functional_groups"
  
  bigquery:
    table_id: "mpg-data-warehouse.vegetation_gridVeg_summaries.gridVeg_plant_functional_groups"
    project: "mpg-data-warehouse"

# GridVeg Species Richness Append Configuration
# Used by: notebooks/append_csv_to_bq.ipynb
gridveg_species_richness_append:
  gcs:
    csv_url: "gs://mpg-data-warehouse/gridVeg/src/2025/gridVeg_species_richness_WRANGLE-251104.csv"
    backup_bucket: "mpg-data-warehouse-backups"
    backup_prefix: "backups/vegetation_gridVeg_summaries/gridVeg_species_richness"
  
  bigquery:
    table_id: "mpg-data-warehouse.vegetation_gridVeg_summaries.gridVeg_species_richness"
    project: "mpg-data-warehouse"

# GridVeg Ground Cover Intercepts Append Configuration
# Used by: notebooks/append_csv_to_bq.ipynb
gridveg_groundcover_append:
  gcs:
    csv_url: "gs://mpg-data-warehouse/gridVeg/src/2025/gridVeg_groundCover_intercepts_WRANGLE-251104.csv"
    backup_bucket: "mpg-data-warehouse-backups"
    backup_prefix: "backups/vegetation_gridVeg_summaries/gridVeg_groundCover_intercepts"
  
  bigquery:
    table_id: "mpg-data-warehouse.vegetation_gridVeg_summaries.gridVeg_groundCover_intercepts"
    project: "mpg-data-warehouse"

